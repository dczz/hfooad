我们已经封装好吉他的参数了
但是我在为吉他添加参数时，也需要对search访问进行修改，吉他规格参数耦合了仓库

* 我们需要封装吉他规格，将它隔离与搜索工具之外
即使你只是增加特性到GuitarSpec类，还是有两个其他类必须被修改（Inventory，Guitar的够惨参数）

* 增加numStrings特性和getNumStrings到GuitarSpec
* 修改Guitar以为让GuitarSpec的特性从此类的构造函数中被封装起来
* 改变Inventory中的search方法，讲两个GuitarSpec对象的对比委托给GuitarSpec，而不是直接处理该对比
q:什么是委托:
a:委托是之讲当前对象需要执行的某项工作时不应该直接进行工作，而是要求另一个对象代为处理（或者有时只是针对部分的工作）。
q:重点是什么？
a:委托让你的程序代码更能重复利用，也让每个对象更关注自己的功能，而不是把处理单一对象行为的程序代码分散在整个应用程序
java里最普通的委托范例之一便是equals方法，不是在方法中考量两个对象是否相等，二十调用两个对象之一的equals并传入第二个对象，
接着从equals方法中得到true活false的相应
q:委托与程序代码之间比较，有何关系?（equals举例)
a:委托让每个对象自己去担心相等性（或者其他工作），这表示你的对象彼此独立或者是比较低耦合，低耦合的对象能从一个应用程序中被取出，而后轻易的在另一个应用程序中被重用。因为他们并未紧密联系到其他对象的程序代码
* 更新FindGuitarTester，让他使用你的新类，